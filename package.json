{
  "name": "orbit-crafter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.38",
    "typescript": "~6.0.2",
    "vite": "^8.0.10",
    "vitest": "^3.0.0"
  }
}
