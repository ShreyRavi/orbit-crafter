# OrbitCraft TODOs

## Design System

- [x] **Extract Visual Language to DESIGN.md** — Done. `DESIGN.md` written with full token set, glow rendering recipe, HUD chrome spec, overlay composite rules.

## Features (deferred from v1)

- [ ] **Animated merge splat** — Upgrade instant-merge + white-pulse collision to an animated splat: debris particles, ripple effect, or slow merge animation. Build after core feel (Step 5) is validated. Blocked by: Step 9 (collision) completion.
